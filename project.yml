name: swift_property_wrapper_demo
options:
  bundleIdPrefix: com.freewind
  deploymentTarget:
    macOS: "14.0"
settings:
  base:
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
targets:
  swift_property_wrapper_demo:
    type: application
    platform: macOS
    productName: swift_property_wrapper_demo
    sources:
      - Sources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.freewind.swiftpropertywrapperdemo
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGN_IDENTITY: "-"
        CODE_SIGN_STYLE: Manual
        DEVELOPMENT_TEAM: ""
        ENABLE_HARDENED_RUNTIME: NO
    scheme:
      testTargets: []
schemes:
  swift_property_wrapper_demo:
    build:
      targets:
        swift_property_wrapper_demo: all
    run:
      config: Debug
